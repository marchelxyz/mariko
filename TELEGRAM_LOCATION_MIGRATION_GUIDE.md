# Руководство по миграции на LocationManager API

## Обзор изменений

Код был обновлен для использования нового **LocationManager API** (Bot API 8.0+) с автоматическим fallback на старый `requestLocation` API (Bot API 6.0+) для максимальной совместимости.

## Что изменилось

### ✅ Улучшения

1. **Новый LocationManager API** - используется по умолчанию для Bot API 8.0+
2. **Автоматический fallback** - старый API используется автоматически, если новый недоступен
3. **Информация о точности** - теперь доступна точность местоположения (`horizontalAccuracy`)
4. **Контроль разрешений** - новые функции для проверки и запроса разрешений
5. **События** - автоматическая обработка событий LocationManager в `_app.tsx`

### 📝 Новые функции

#### `checkLocationPermission()`
Проверяет статус разрешений на геолокацию (только для Bot API 8.0+):

```typescript
import { checkLocationPermission } from '@/lib/location';

const status = await checkLocationPermission();
if (status) {
  console.log('Доступна:', status.isLocationAvailable);
  console.log('Запрошено:', status.isPermissionRequested);
  console.log('Предоставлено:', status.isPermissionGranted);
}
```

#### `openLocationSettings()`
Открывает настройки для запроса разрешения на геолокацию:

```typescript
import { openLocationSettings } from '@/lib/location';

// ВАЖНО: можно вызвать только в ответ на действие пользователя (клик по кнопке)
const opened = await openLocationSettings();
if (opened) {
  console.log('Настройки открыты');
}
```

### 🔄 Обратная совместимость

Функция `requestLocation()` работает так же, как и раньше, но теперь:
- Использует LocationManager API, если доступен (Bot API 8.0+)
- Автоматически fallback на старый API (Bot API 6.0+)
- Возвращает дополнительное поле `horizontalAccuracy` (может быть `null`)

```typescript
import { requestLocation } from '@/lib/location';

const location = await requestLocation();
if (location) {
  console.log(location.latitude, location.longitude);
  // Новое поле (может быть null для старого API)
  console.log('Точность:', location.horizontalAccuracy);
}
```

## Примеры использования

### Пример 1: Простой запрос местоположения

```typescript
import { requestLocation } from '@/lib/location';

const location = await requestLocation();
if (location) {
  // Используем координаты
  console.log(`Координаты: ${location.latitude}, ${location.longitude}`);
} else {
  // Пользователь отказал или произошла ошибка
  console.log('Местоположение недоступно');
}
```

### Пример 2: Проверка разрешений перед запросом

```typescript
import { requestLocation, checkLocationPermission, openLocationSettings } from '@/lib/location';

async function requestLocationWithPermissionCheck() {
  // Проверяем разрешения (только для Bot API 8.0+)
  const permissionStatus = await checkLocationPermission();
  
  if (permissionStatus) {
    if (!permissionStatus.isLocationAvailable) {
      console.log('Геолокация недоступна на этом устройстве');
      return null;
    }
    
    if (!permissionStatus.isPermissionGranted) {
      console.log('Разрешение не предоставлено');
      // Можно показать кнопку пользователю для запроса разрешения
      // и вызвать openLocationSettings() при клике
      return null;
    }
  }
  
  // Запрашиваем местоположение
  return await requestLocation();
}
```

### Пример 3: Кнопка для запроса разрешения

```typescript
import { openLocationSettings, requestLocation } from '@/lib/location';

function LocationButton() {
  const handleRequestLocation = async () => {
    // Сначала пробуем получить местоположение
    let location = await requestLocation();
    
    // Если не получилось, возможно нужно запросить разрешение
    if (!location) {
      // Открываем настройки (можно вызвать только в ответ на действие пользователя)
      const opened = await openLocationSettings();
      if (opened) {
        console.log('Настройки открыты, пользователь может предоставить разрешение');
        // После того как пользователь вернется из настроек,
        // можно попробовать запросить местоположение снова
      }
    } else {
      console.log('Местоположение получено:', location);
    }
  };
  
  return (
    <button onClick={handleRequestLocation}>
      Определить местоположение
    </button>
  );
}
```

## Технические детали

### Инициализация LocationManager

LocationManager автоматически инициализируется при загрузке приложения в `_app.tsx`. Это обеспечивает:
- Лучшую производительность при первом запросе местоположения
- Автоматическую обработку событий `locationManagerUpdated` и `locationRequested`

### Обработка событий

События LocationManager обрабатываются автоматически в `_app.tsx`:
- `locationManagerUpdated` - при изменении состояния LocationManager
- `locationRequested` - при запросе местоположения

### Fallback логика

1. Пробуется LocationManager API (Bot API 8.0+)
2. Если недоступен, используется старый `requestLocation` API (Bot API 6.0+)
3. Если оба недоступны, возвращается `null`

## Рекомендации

1. **Всегда проверяйте результат** - `requestLocation()` может вернуть `null`
2. **Используйте `checkLocationPermission()`** для лучшего UX (только Bot API 8.0+)
3. **Вызывайте `openLocationSettings()` только в ответ на действие пользователя** - это требование Telegram
4. **Используйте `horizontalAccuracy`** для фильтрации неточных данных (если доступно)
5. **Кэшируйте местоположение** - используйте `getStoredLocation()` и `storeLocation()` для уменьшения количества запросов

## Совместимость

- ✅ Bot API 8.0+ - полная поддержка LocationManager
- ✅ Bot API 6.0-7.9 - автоматический fallback на старый API
- ✅ Старые версии - возвращается `null` с предупреждением в консоли

## Дополнительная информация

- [Полная документация Telegram Mini Apps](./TELEGRAM_MINI_APPS_DOCUMENTATION.md)
- [Документация по геолокации](./LOCATION_FEATURE.md)
- [Официальная документация Telegram](https://core.telegram.org/bots/webapps#locationmanager)
