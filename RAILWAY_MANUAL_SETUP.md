# Инструкции по ручной настройке на Railway

Этот документ содержит инструкции по настройке улучшений производительности, которые нужно выполнить вручную в Railway Dashboard.

## ⚠️ Важно: Влияние на другие проекты

**Все настройки ниже применяются ТОЛЬКО к текущему проекту и НЕ влияют на другие проекты в вашем аккаунте Railway.**

- Пул соединений БД настроен в коде и влияет только на этот проект
- Rate Limiting настроен в коде и влияет только на этот проект
- Автомасштабирование настраивается отдельно для каждого сервиса
- Redis будет отдельным сервисом, не влияющим на другие проекты

---

## 1. Настройка переменных окружения для пула соединений БД

### Для Starter плана (по умолчанию):
Ничего делать не нужно. Код использует безопасное значение по умолчанию: **30 соединений**.

### Для Pro плана:
1. Откройте Railway Dashboard
2. Перейдите в ваш проект → Backend сервис
3. Откройте вкладку **Variables**
4. Добавьте новую переменную:
   - **Key**: `DB_POOL_MAX`
   - **Value**: `100`
5. Сохраните изменения
6. Railway автоматически перезапустит сервис

**Результат**: Пул соединений увеличится с 30 до 100, что позволит обрабатывать больше одновременных запросов.

---

## 2. Настройка Rate Limiting (опционально)

### Для Starter плана:
Ничего делать не нужно. Код использует безопасное значение по умолчанию: **100 запросов за 15 минут**.

### Для Pro плана (если нужно увеличить лимит):
1. Откройте Railway Dashboard
2. Перейдите в ваш проект → Backend сервис
3. Откройте вкладку **Variables**
4. Добавьте новую переменную:
   - **Key**: `RATE_LIMIT_API_MAX`
   - **Value**: `200` (или другое значение по вашему усмотрению)
5. Сохраните изменения
6. Railway автоматически перезапустит сервис

**Результат**: Лимит запросов увеличится с 100 до 200 за 15 минут.

---

## 3. Автомасштабирование (только для Pro плана)

⚠️ **Важно**: Автомасштабирование доступно только на Pro плане Railway.

### Настройка автомасштабирования:

1. Откройте Railway Dashboard
2. Перейдите в ваш проект → Backend сервис
3. Откройте вкладку **Settings**
4. Найдите раздел **Scaling** или **Auto-scaling**
5. Включите **Auto-scaling** (если доступно)
6. Настройте параметры:
   - **Min instances**: `1` (минимум инстансов)
   - **Max instances**: `4` (максимум инстансов)
   - **CPU threshold**: `70%` (создавать новый инстанс при 70% CPU)
   - **Memory threshold**: `80%` (создавать новый инстанс при 80% памяти)

**Результат**: Railway будет автоматически создавать дополнительные инстансы при росте нагрузки и удалять их при снижении.

**Влияние на другие проекты**: Нет. Автомасштабирование настраивается отдельно для каждого сервиса.

---

## 4. Установка Redis для кэширования (опционально)

⚠️ **Важно**: Redis - это отдельный сервис, который не влияет на другие проекты. Вы можете использовать его только для этого проекта или не использовать вообще.

### Установка Redis:

1. Откройте Railway Dashboard
2. Перейдите в ваш проект
3. Нажмите **+ New** → **Database** → **Add Redis**
4. Railway автоматически создаст Redis инстанс
5. После создания Redis, Railway автоматически добавит переменную окружения `REDIS_URL` в ваш Backend сервис
6. Перезапустите Backend сервис (Railway сделает это автоматически при изменении переменных окружения)

### Настройка кэширования в коде (если нужно):

После установки Redis, вы можете добавить кэширование в код. См. раздел 6 в `PERFORMANCE_GUIDE_DETAILED.md`.

**Результат**: Значительное увеличение производительности для часто запрашиваемых данных (меню, рестораны).

**Влияние на другие проекты**: Нет. Redis - отдельный сервис, который используется только этим проектом.

---

## 5. Проверка работы улучшений

### Проверка пула соединений:

После деплоя, проверьте логи Backend сервиса. Вы должны увидеть:
```
📊 Настройки пула соединений: { max: 30, min: 5, ... }
```

### Проверка Rate Limiting:

1. Откройте ваш API в браузере или Postman
2. Отправьте более 100 запросов за 15 минут с одного IP
3. После превышения лимита вы получите ошибку 429 с сообщением о превышении лимита

### Проверка индексов БД:

Индексы будут созданы автоматически при следующем деплое благодаря `synchronize: true` в TypeORM.

Для проверки можно подключиться к БД и выполнить:
```sql
-- Проверить индексы таблицы restaurants
SELECT indexname, indexdef 
FROM pg_indexes 
WHERE tablename = 'restaurants';

-- Проверить индексы таблицы menu_items
SELECT indexname, indexdef 
FROM pg_indexes 
WHERE tablename = 'menu_items';
```

### Проверка мониторинга:

1. Авторизуйтесь как администратор
2. Откройте эндпоинт: `GET /api/admin/metrics`
3. Вы увидите метрики производительности всех маршрутов

---

## Итоговая сводка: что нужно сделать вручную

### Обязательно (для максимальной производительности):

1. ✅ **Для Pro плана**: Установить `DB_POOL_MAX=100` в переменных окружения
2. ✅ **Для Pro плана**: Настроить автомасштабирование (если доступно)

### Опционально:

3. ⭐ **Для Pro плана**: Установить `RATE_LIMIT_API_MAX=200` (если нужно больше запросов)
4. ⭐ **Для всех планов**: Установить Redis для кэширования (значительное улучшение производительности)

---

## Что уже сделано автоматически (в коде):

✅ Пул соединений БД настроен (по умолчанию 30, можно изменить через `DB_POOL_MAX`)
✅ Rate Limiting реализован (по умолчанию 100 запросов за 15 минут)
✅ Индексы БД добавлены в модели (создадутся автоматически при деплое)
✅ Мониторинг производительности реализован (эндпоинты `/api/admin/metrics`)

---

## Вопросы и ответы

**Q: Повлияют ли эти настройки на другие проекты в моем аккаунте Railway?**
A: Нет. Все настройки применяются только к текущему проекту. Пул соединений, Rate Limiting и мониторинг настроены в коде и работают только для этого проекта. Автомасштабирование настраивается отдельно для каждого сервиса. Redis - отдельный сервис, который используется только этим проектом.

**Q: Нужно ли что-то делать для Starter плана?**
A: Нет, все настройки по умолчанию безопасны для Starter плана. Вы можете опционально установить Redis для кэширования.

**Q: Как узнать, какой у меня план?**
A: Откройте Railway Dashboard → Settings → Plan. Там будет указан ваш текущий план.

**Q: Что делать, если я не хочу использовать автомасштабирование?**
A: Ничего. Автомасштабирование опционально и доступно только на Pro плане. Без него приложение будет работать на одном инстансе.

**Q: Обязательно ли устанавливать Redis?**
A: Нет, Redis опционален. Без него приложение будет работать нормально, но с Redis производительность будет значительно выше для часто запрашиваемых данных.
