# Руководство по диагностике проблем запуска

## Улучшенное логирование

Приложение теперь выводит детальную информацию на каждом этапе запуска, что помогает быстро определить причину проблем.

## Что было улучшено

### 1. Детальное логирование запуска
- Время выполнения каждого этапа
- Проверка наличия переменных окружения
- Детальная диагностика ошибок подключения к БД
- Информация о времени запуска и использовании ресурсов

### 2. Улучшенный Health Check
Эндпоинт `/health` теперь возвращает:
- Статус подключения к БД с количеством активных соединений
- Статус Redis
- Использование памяти
- Время ответа health check
- Uptime приложения

### 3. Детальная диагностика ошибок БД
При ошибках подключения к базе данных выводятся конкретные рекомендации:
- **Таймаут**: Проверка доступности хоста, порта, сетевых настроек
- **Connection refused**: Проверка запуска PostgreSQL сервиса
- **Аутентификация**: Проверка пароля и прав доступа
- **База данных не существует**: Инструкции по созданию БД
- **SSL ошибки**: Проверка настроек SSL

### 4. Улучшенный Graceful Shutdown
При получении SIGTERM/SIGINT:
- Логируется время работы приложения до shutdown
- Последовательное закрытие всех соединений с таймингами
- Детальная информация о каждом шаге закрытия

## Как использовать для диагностики

### Проблема: Приложение не запускается

1. **Проверьте логи запуска** - они покажут на каком этапе происходит ошибка:
   ```
   🚀 ЗАПУСК СЕРВЕРА
   📊 ШАГ 1: Подключение к базе данных...
   ```

2. **Если ошибка на этапе подключения к БД**, проверьте:
   - Наличие переменной `DATABASE_URL` в Railway
   - Правильность формата `DATABASE_URL`
   - Доступность PostgreSQL сервиса в Railway

3. **Если приложение получает SIGTERM до запуска**, возможные причины:
   - Health check не проходит (проверьте `/health` endpoint)
   - Таймаут запуска (Railway убивает контейнер, если он не отвечает вовремя)
   - Недостаточно ресурсов

### Проблема: "Connection reset by peer" в логах PostgreSQL

Это означает, что:
- Приложение получило SIGTERM и начало закрывать соединения
- PostgreSQL видит разорванные соединения от клиента
- Это нормально при graceful shutdown

**Если это происходит при запуске**, значит:
- Приложение не успевает подключиться к БД
- Railway убивает контейнер из-за таймаута health check
- Проверьте логи запуска для точной причины

### Проверка Health Check

```bash
curl https://your-app.railway.app/health
```

Ответ должен содержать:
```json
{
  "status": "ok",
  "database": {
    "status": "connected",
    "activeConnections": 5,
    "idleConnections": 0
  },
  "redis": {
    "status": "connected"
  },
  "memory": {
    "used": "45MB",
    "total": "67MB",
    "rss": "123MB"
  }
}
```

Если `status: "unhealthy"` или `database.status: "disconnected"`:
- База данных не подключена
- Проверьте логи запуска для деталей

## Типичные проблемы и решения

### 1. Таймаут подключения к БД
**Симптомы**: Логи показывают "Database connection timeout"
**Решение**:
- Проверьте, что PostgreSQL сервис запущен в Railway
- Проверьте правильность `DATABASE_URL`
- Увеличьте таймаут (по умолчанию 30 секунд)

### 2. База данных недоступна
**Симптомы**: "ECONNREFUSED" или "connection refused"
**Решение**:
- Убедитесь, что PostgreSQL сервис добавлен в проект Railway
- Проверьте, что сервис запущен (не в режиме паузы)
- Проверьте правильность хоста и порта в `DATABASE_URL`

### 3. Ошибка аутентификации
**Симптомы**: "password authentication failed"
**Решение**:
- Проверьте правильность пароля в `DATABASE_URL`
- Убедитесь, что Railway автоматически подставил правильный `DATABASE_URL`
- Если используете отдельные переменные, проверьте `DB_USER` и `DB_PASSWORD`

### 4. Приложение получает SIGTERM сразу после запуска
**Симптомы**: Логи показывают запуск, но затем сразу SIGTERM
**Решение**:
- Проверьте настройки health check в Railway
- Убедитесь, что `/health` endpoint доступен и отвечает быстро
- Проверьте логи на наличие ошибок перед SIGTERM
- Увеличьте таймаут health check в настройках Railway

## Логи для отправки в поддержку

При обращении за помощью приложите:
1. Полные логи запуска (первые 50-100 строк)
2. Ответ от `/health` endpoint
3. Настройки переменных окружения (без паролей)
4. Время, когда произошла ошибка

## Мониторинг

Рекомендуется настроить мониторинг:
- Health check endpoint (`/health`)
- Логи Railway на наличие ошибок
- Метрики использования памяти и CPU
- Количество активных соединений к БД
