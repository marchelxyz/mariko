# Марико - Telegram Mini App для ресторана

Telegram Mini App для управления ресторанами с функционалом бронирования столиков, заказа доставки, управления меню и админ-панелью.

## 🏗️ Архитектура проекта

Проект разделен на два основных модуля:
- **Frontend** (`/frontend`) - Next.js приложение для Telegram Mini App
- **Backend** (`/backend`) - Express.js API сервер

## 🚀 Быстрый старт

### Локальная разработка

1. Установите зависимости:
```bash
npm install
cd frontend && npm install
cd ../backend && npm install
```

2. Запустите приложение в режиме разработки:
```bash
npm run dev
```

Frontend будет доступен на `http://localhost:3000`
Backend будет доступен на `http://localhost:5000`

### Переменные окружения

#### Backend (Railway)
- `PORT` - Порт сервера (по умолчанию 5000)
- `MONGODB_URI` - URI подключения к MongoDB
- `JWT_SECRET` - Секретный ключ для JWT токенов
- `FRONTEND_URL` - URL фронтенда для CORS

#### Frontend (Vercel)
- `NEXT_PUBLIC_API_URL` - URL бекенд API

## 📁 Структура проекта

```
mariko-restaurant-tma/
├── frontend/                 # Next.js приложение
│   ├── components/          # React компоненты
│   ├── pages/               # Страницы приложения
│   ├── store/               # Zustand store для состояния
│   ├── lib/                 # Утилиты и API клиент
│   └── styles/              # Глобальные стили
├── backend/                 # Express.js API
│   ├── src/
│   │   ├── config/          # Конфигурация (БД и т.д.)
│   │   ├── models/          # Mongoose модели
│   │   ├── routes/          # API роуты
│   │   ├── middleware/      # Express middleware
│   │   └── index.ts         # Точка входа
└── README.md
```

## 🎨 Цветовая палитра

- **Основной цвет**: `#8E1A1A`
- **Белый**: `#FFFFFF`
- **Черный**: `#000000`
- **Вспомогательные**:
  - `#F3E5D2` - Светлый бежевый
  - `#B05E5E` - Акцентный красный
  - `#F7F7F7` - Светло-серый фон

## 📱 Функционал

### Для пользователей:
- ✅ Главная страница с меню кнопок действий
- ✅ Выбор ресторана (разные города)
- ✅ Просмотр меню ресторана
- ✅ Бронирование столика
- ✅ Заказ доставки
- ✅ Оставление отзывов
- ✅ Просмотр информации о франшизе
- ✅ Профиль пользователя с редактированием данных

### Для администраторов:
- ✅ Управление баннерами
- ✅ Управление ролями пользователей
- ✅ Настройка рассылок
- ✅ Добавление изображений блюд

## 🔐 Роли пользователей

- **user** - Обычный пользователь
- **admin** - Главный администратор (полный доступ)
- **marketing** - Маркетолог (рассылки и баннеры)
- **manager** - Управляющий рестораном

## 🚢 Деплой на Railway

### Backend (Railway)

1. Создайте новый проект на Railway (https://railway.app)
2. Подключите репозиторий GitHub
3. В настройках проекта установите:
   - **Root Directory**: `backend`
   - **Build Command**: `npm install && npm run build`
   - **Start Command**: `npm start`

4. Установите переменные окружения в Railway:
   - `MONGODB_URI` - URI подключения к MongoDB (можно использовать Railway MongoDB плагин)
   - `JWT_SECRET` - Секретный ключ для JWT (сгенерируйте случайную строку)
   - `FRONTEND_URL` - URL вашего Vercel приложения (например: https://your-app.vercel.app)
   - `PORT` - Railway установит автоматически

5. Railway автоматически задеплоит приложение

**Команда для запуска на Railway (автоматически):**
```bash
cd backend && npm install && npm run build && npm start
```

### Frontend (Vercel)

1. Создайте новый проект на Vercel (https://vercel.com)
2. Подключите репозиторий GitHub
3. В настройках проекта установите:
   - **Root Directory**: `frontend`
   - **Framework Preset**: Next.js

4. Установите переменные окружения в Vercel:
   - `NEXT_PUBLIC_API_URL` - URL вашего Railway бекенда (например: https://your-backend.railway.app)

5. Vercel автоматически соберет и задеплоит Next.js приложение

## 📝 API Endpoints

### Аутентификация
- `POST /api/auth/telegram` - Авторизация через Telegram

### Рестораны
- `GET /api/restaurants` - Список ресторанов
- `GET /api/restaurants/:id` - Информация о ресторане

### Меню
- `GET /api/menu/:restaurantId` - Меню ресторана

### Баннеры
- `GET /api/banners` - Список баннеров
- `POST /api/banners` - Создать баннер (требует авторизации)
- `PUT /api/banners/:id` - Обновить баннер (требует авторизации)
- `DELETE /api/banners/:id` - Удалить баннер (требует авторизации)

### Профиль
- `GET /api/profile` - Получить профиль (требует авторизации)
- `PUT /api/profile` - Обновить профиль (требует авторизации)

### Бронирование
- `POST /api/booking` - Создать бронирование (требует авторизации)

### Админ панель
- `GET /api/admin/banners` - Управление баннерами
- `GET /api/admin/users` - Список пользователей
- `PUT /api/admin/users/:id/role` - Изменить роль пользователя

## 🛠️ Технологии

### Frontend:
- Next.js 14
- React 18
- TypeScript
- Tailwind CSS
- Zustand (state management)
- @twa-dev/sdk (Telegram WebApp SDK)

### Backend:
- Express.js
- TypeScript
- MongoDB + Mongoose
- JWT аутентификация
- Zod валидация

## 📋 TODO

- [ ] Интеграция Google Sheets для управления меню
- [ ] Реализация функционала бронирования столиков
- [ ] Реализация заказа доставки
- [ ] Система отзывов
- [ ] Рассылки пользователям
- [ ] Загрузка изображений блюд
- [ ] Валидация Telegram initData

## 📄 Лицензия

Private project
