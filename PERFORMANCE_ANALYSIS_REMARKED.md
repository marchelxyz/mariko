# Анализ производительности запросов к ReMarked API

## 📊 Анализ логов от 02.12.2025

### Проблемы производительности

#### 1. Медленные GET запросы (2+ секунды)

**GET /api/booking/slots** - 2147ms
- Статус: 304 (Not Modified) ✅
- Проблема: Запрос к внешнему API ReMarked занимает ~2 секунды

**GET /api/restaurants/.../tables** - 2250ms  
- Статус: 304 (Not Modified) ✅
- Проблема: Вызов `getTimesWithTables()` к ReMarked API занимает ~2 секунды

#### 2. Медленный POST запрос (1.3+ секунды)

**POST /api/booking** - 1341ms
- Статус: 201 (Created) ✅
- Детальная статистика:
  - `cache_lookup`: 1ms ✅
  - `db_restaurant`: 0ms ✅
  - `remarked_token`: 0ms ✅ (кешируется)
  - `remarked_create`: **1337ms** ⚠️ (99.9% времени!)

### Выводы

1. **Внутренние операции быстрые** - кеширование и БД работают отлично
2. **Основная проблема - внешний API ReMarked** - все медленные запросы связаны с ним
3. **Токены кешируются** - `remarked_token: 0ms` показывает эффективность кеширования

## 🔍 Причины медленной работы

### Возможные причины медленной работы ReMarked API:

1. **Сетевая задержка** - географическое расстояние до серверов ReMarked
2. **Нагрузка на серверах ReMarked** - высокая нагрузка в пиковые часы
3. **Сложные вычисления** - `getTimesWithTables` и `getSlots` требуют обработки большого объема данных
4. **Отсутствие кеширования** - результаты запросов не кешируются на нашей стороне

## 💡 Рекомендации по оптимизации

### 1. Добавить кеширование результатов запросов (высокий приоритет)

**Для GET запросов:**
- `getSlots()` - кешировать на 1-2 минуты (данные могут изменяться быстро)
- `getTimesWithTables()` - кешировать на 1-2 минуты

**Преимущества:**
- Снижение нагрузки на ReMarked API
- Улучшение времени отклика для повторных запросов
- Снижение риска rate limiting

**Реализация:**
```typescript
// Добавить в cacheService.ts
REMARKED_SLOTS: 'remarked:slots',
REMARKED_TABLES: 'remarked:tables',

// TTL: 60-120 секунд (1-2 минуты)
```

### 2. Оптимизировать таймауты (средний приоритет)

Текущий таймаут: 30 секунд (30000ms)
- Для медленных запросов это нормально
- Но можно добавить более короткие таймауты для быстрых операций

### 3. Добавить retry логику (средний приоритет)

Для критичных операций (создание бронирования):
- Повторять запрос при временных ошибках (5xx)
- Экспоненциальная задержка между попытками
- Максимум 2-3 попытки

### 4. Асинхронная обработка (низкий приоритет)

Для некритичных операций:
- Отправлять ответ клиенту сразу
- Обрабатывать данные в фоне
- Использовать очереди (Redis/BullMQ)

### 5. Мониторинг и алерты (высокий приоритет)

- Настроить алерты при запросах > 3 секунд
- Отслеживать процент медленных запросов
- Мониторить доступность ReMarked API

## 📈 Ожидаемые улучшения

После внедрения кеширования:
- **GET /api/booking/slots**: 2147ms → **~50-100ms** (при кеш-попадании)
- **GET /api/restaurants/.../tables**: 2250ms → **~50-100ms** (при кеш-попадании)
- **POST /api/booking**: 1341ms → **~1341ms** (без изменений, т.к. нельзя кешировать создание)

## ⚠️ Ограничения

1. **POST /api/booking** нельзя оптимизировать через кеширование - это операция записи
2. Медленная работа ReMarked API - внешний фактор, который мы не можем контролировать
3. Кеширование может привести к устаревшим данным (но это приемлемо для короткого TTL)

## 🎯 План действий

1. ✅ Добавлено детальное логирование в `remarkedService.ts`
2. ⏳ Добавить кеширование для `getSlots()` и `getTimesWithTables()`
3. ⏳ Настроить мониторинг медленных запросов
4. ⏳ Рассмотреть retry логику для критичных операций

## 📝 Дополнительные заметки

- Статус 304 (Not Modified) показывает, что HTTP кеширование работает на уровне клиента
- Redis кеширование работает эффективно (токены кешируются)
- Проблема не в нашем коде, а в скорости внешнего API
