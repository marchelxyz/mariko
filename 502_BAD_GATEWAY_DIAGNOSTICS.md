# Диагностика проблемы 502 Bad Gateway

## Симптомы
- Запросы доходят до Railway прокси
- Railway возвращает 502 Bad Gateway с сообщением "Application failed to respond"
- Нет логов обработки запросов на бекенде

## Что означает 502 Bad Gateway

502 означает, что Railway прокси не может получить ответ от вашего бекенд-приложения. Это может произойти по следующим причинам:

### 1. Бекенд не запущен или падает при старте (наиболее вероятно)

**Проверка:**
1. Откройте Railway Dashboard → Ваш бекенд сервис → Logs
2. Проверьте, есть ли логи запуска сервера
3. Ищите сообщения:
   - `✅ СЕРВЕР УСПЕШНО ЗАПУЩЕН`
   - `🚀 Server running on port ${PORT}`
   - Ошибки подключения к БД
   - Ошибки инициализации

**Решение:**
- Если бекенд не запускается, проверьте логи ошибок
- Убедитесь, что все переменные окружения установлены
- Проверьте подключение к БД

### 2. Таймаут запроса (Railway имеет таймаут ~30 секунд)

**Проблема:** Запрос обрабатывается слишком долго, Railway закрывает соединение.

**Проверка:**
- В логах бекенда должны быть сообщения о начале обработки запроса
- Но запрос не завершается в течение 30 секунд

**Решение:**
- Добавлен middleware для обработки таймаутов (25 секунд)
- Оптимизируйте запросы к БД
- Используйте кэширование для тяжелых запросов

### 3. Бекенд падает при обработке запроса

**Проблема:** Бекенд получает запрос, но падает во время обработки.

**Проверка:**
- В логах должны быть сообщения:
  - `[pages/home] 📥 Начало обработки запроса /home`
  - Но затем ошибка или падение

**Решение:**
- Проверьте логи на наличие ошибок
- Убедитесь, что БД доступна и инициализирована
- Проверьте, что все зависимости установлены

### 4. Проблемы с подключением к БД

**Проблема:** Бекенд не может подключиться к БД, запросы зависают.

**Проверка:**
- В логах должны быть ошибки подключения к БД
- Или запросы зависают на этапе обращения к БД

**Решение:**
- Проверьте `DATABASE_URL` в Railway
- Убедитесь, что БД доступна
- Проверьте лимиты подключений к БД

## Что было добавлено для диагностики

### 1. Детальное логирование в `/api/pages/home`
- ✅ Логирование начала обработки запроса
- ✅ Логирование проверки кэша
- ✅ Логирование загрузки данных из БД
- ✅ Логирование времени выполнения каждого этапа
- ✅ Детальное логирование ошибок

### 2. Проверка инициализации БД
- ✅ Проверка перед обработкой запроса
- ✅ Возврат 503 если БД не инициализирована

### 3. Обработка ошибок кэша
- ✅ Если Redis недоступен, запрос продолжается без кэша
- ✅ Не падает приложение при ошибках кэша

### 4. Middleware для таймаутов
- ✅ Автоматическое завершение запросов через 25 секунд
- ✅ Возврат 504 Gateway Timeout вместо 502

## Шаги диагностики

### Шаг 1: Проверка логов бекенда

1. Откройте Railway Dashboard
2. Перейдите в ваш бекенд сервис
3. Откройте вкладку Logs
4. Ищите:
   - Сообщения о запуске сервера
   - Сообщения о входящих запросах: `📥 Входящий запрос: GET /api/pages/home`
   - Сообщения о начале обработки: `[pages/home] 📥 Начало обработки запроса /home`
   - Ошибки и stack traces

### Шаг 2: Проверка health check

```bash
curl https://mariko-production.up.railway.app/health
```

Должен вернуть JSON с информацией о здоровье сервера. Если возвращает 502 - бекенд не запущен.

### Шаг 3: Проверка переменных окружения

В Railway Dashboard проверьте:
- `DATABASE_URL` - должен быть установлен
- `JWT_SECRET` - должен быть установлен
- `FRONTEND_URL` - должен быть установлен
- `PORT` - обычно устанавливается автоматически Railway

### Шаг 4: Проверка подключения к БД

В логах бекенда должны быть сообщения:
- `✅ База данных подключена за Xms`
- Или ошибки подключения к БД

### Шаг 5: Проверка обработки запросов

После добавления логирования, в логах должны быть:
```
[timestamp] 📥 Входящий запрос: GET /api/pages/home
[pages/home] 📥 Начало обработки запроса /home
[pages/home] 🔄 Попытка получить данные из кэша...
[pages/home] 🔄 Загрузка данных из БД...
[pages/home] ✅ Данные загружены из БД за Xms
[pages/home] ✅ Запрос успешно обработан за Xms
```

Если видите только начало обработки, но нет завершения - проблема в обработке запроса.

## Типичные проблемы и решения

### Проблема: Бекенд не запускается

**Симптомы:**
- Нет логов запуска сервера
- Логи показывают ошибки при старте

**Решение:**
1. Проверьте логи ошибок
2. Убедитесь, что все переменные окружения установлены
3. Проверьте подключение к БД
4. Проверьте, что порт правильный (Railway устанавливает PORT автоматически)

### Проблема: Таймаут запросов

**Симптомы:**
- Запросы начинают обрабатываться, но не завершаются
- В логах видно начало обработки, но нет завершения

**Решение:**
1. Оптимизируйте запросы к БД (добавьте индексы)
2. Используйте кэширование для тяжелых запросов
3. Разбейте большие запросы на несколько маленьких
4. Проверьте производительность БД

### Проблема: БД не доступна

**Симптомы:**
- Ошибки подключения к БД в логах
- Запросы зависают на этапе обращения к БД

**Решение:**
1. Проверьте `DATABASE_URL` в Railway
2. Убедитесь, что БД запущена и доступна
3. Проверьте лимиты подключений к БД
4. Проверьте сетевые настройки Railway

### Проблема: Ошибки в коде

**Симптомы:**
- В логах видны ошибки и stack traces
- Запросы падают с ошибками

**Решение:**
1. Проверьте логи на наличие ошибок
2. Исправьте ошибки в коде
3. Добавьте обработку ошибок для всех async операций

## Что делать дальше

1. **Проверьте логи бекенда в Railway**
   - Найдите сообщения о запуске сервера
   - Найдите сообщения о входящих запросах
   - Найдите ошибки

2. **Проверьте health check**
   ```bash
   curl https://mariko-production.up.railway.app/health
   ```

3. **Проверьте переменные окружения**
   - Убедитесь, что все необходимые переменные установлены

4. **Проверьте подключение к БД**
   - Убедитесь, что БД доступна
   - Проверьте `DATABASE_URL`

5. **Если проблема не решена**
   - Соберите логи бекенда
   - Соберите логи Railway
   - Проверьте метрики Railway (CPU, память, сеть)

## Дополнительная информация

После добавления логирования, вы должны видеть в логах:
- Все входящие запросы с деталями
- Начало и завершение обработки каждого запроса
- Время выполнения каждого этапа
- Детальные ошибки с stack traces

Это поможет точно определить, где именно происходит проблема.
