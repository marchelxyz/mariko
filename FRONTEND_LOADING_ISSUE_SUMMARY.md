# Резюме: Проблема с загрузкой контента

## Симптомы
✅ Интерфейс загружается (HTML рендерится)  
❌ Контент не загружается (меню, баннеры, рестораны)  
❌ Нет логов запросов на бекенде

## Наиболее вероятные причины

### 1. ❗ NEXT_PUBLIC_API_URL не установлена в production (90% вероятность)

**Что происходит:**
- SSR запросы (из `getServerSideProps`) работают, т.к. выполняются на сервере Next.js
- Клиентские запросы не работают, т.к. используют `localhost` вместо реального URL бекенда

**Как проверить:**
1. Откройте консоль браузера (F12)
2. Найдите сообщение: `🔗 API Base URL: ...`
3. Если видите `http://localhost:5000/api` в production - проблема подтверждена

**Решение:**
```bash
# В Vercel Dashboard:
# Settings → Environment Variables → Add:
NEXT_PUBLIC_API_URL=https://your-backend.railway.app
# (БЕЗ /api в конце, код сам добавит)
```

### 2. CORS блокирует запросы (5% вероятность)

**Что происходит:**
- Запросы отправляются, но браузер их блокирует
- В Network tab видно ошибку CORS

**Как проверить:**
1. Откройте DevTools → Network
2. Найдите запросы к API
3. Если статус `CORS error` или `Failed to fetch` - проблема с CORS

**Решение:**
- Проверьте `FRONTEND_URL` на бекенде
- Убедитесь, что URL фронтенда добавлен в `allowedOrigins`

### 3. Бекенд недоступен (3% вероятность)

**Что происходит:**
- Запросы отправляются, но не доходят до бекенда
- В Network tab видно `ECONNREFUSED` или таймаут

**Как проверить:**
```bash
curl https://your-backend.railway.app/health
```

**Решение:**
- Проверьте, что бекенд запущен
- Проверьте URL бекенда

### 4. JavaScript ошибки (2% вероятность)

**Что происходит:**
- Ошибки в коде предотвращают выполнение запросов
- В консоли браузера видны ошибки

**Как проверить:**
- Откройте консоль браузера (F12)
- Проверьте наличие красных ошибок

**Решение:**
- Исправьте ошибки в коде

## Быстрая диагностика

### Шаг 1: Проверка консоли браузера
```javascript
// Откройте консоль и выполните:
console.log('API URL:', process.env.NEXT_PUBLIC_API_URL);
// Должно вывести URL бекенда, не undefined
```

### Шаг 2: Проверка Network tab
1. F12 → Network
2. Обновите страницу
3. Найдите запросы к `/api/restaurants`, `/api/banners`
4. Проверьте статус и URL запросов

### Шаг 3: Проверка логов бекенда
Должны быть логи вида:
```
[timestamp] 📥 Входящий запрос: GET /api/restaurants
```

Если таких логов нет - запросы не доходят до бекенда.

## Что было добавлено для диагностики

### На бекенде:
- ✅ Детальное логирование всех входящих запросов
- ✅ Логирование Origin, IP, User-Agent
- ✅ Логирование завершения запросов

### На фронтенде:
- ✅ Детальное логирование отправки запросов
- ✅ Логирование URL и переменных окружения
- ✅ Логирование успешных ответов и ошибок

## Следующие шаги

1. **Проверьте переменные окружения в Vercel**
   - Убедитесь, что `NEXT_PUBLIC_API_URL` установлена
   - Пересоберите приложение

2. **Проверьте консоль браузера**
   - Откройте приложение
   - Проверьте логи запросов
   - Найдите ошибки

3. **Проверьте Network tab**
   - Убедитесь, что запросы отправляются
   - Проверьте статус ответов

4. **Проверьте логи бекенда**
   - Убедитесь, что запросы доходят до бекенда
   - Проверьте ошибки на бекенде

## Если проблема не решена

Соберите следующую информацию:
1. Скриншот консоли браузера
2. Скриншот Network tab
3. Логи бекенда
4. Значение `NEXT_PUBLIC_API_URL` из консоли браузера
